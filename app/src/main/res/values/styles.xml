<resources>

    <style name="MyMaterialTheme" parent="MyMaterialTheme.Base">
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:alertDialogTheme">@style/DialogBox</item>
    </style>

    <style name="DialogBox" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:windowBackground">@drawable/dialog_background</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <!--<style name="MyMaterialSplashTheme" parent="MyMaterialTheme.Base">
        <item name="android:windowBackground">@drawable/splash_screen_logo</item>
    </style>!-->

    <style name="MyMaterialTheme.Base" parent="Base.Theme.AppCompat">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/windowBackground</item>
        <item name="colorPrimaryDark">@color/windowBackground</item>
        <item name="android:windowBackground">@drawable/background</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@color/navigationBarColor</item>
    </style>
</resources>